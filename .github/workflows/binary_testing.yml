name: binary-testing
on : [push]
jobs:
  compiling:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: /

    steps:
      - name: checkout
        uses: "actions/checkout@v3"
      - run: ls -l
      - run: make re
