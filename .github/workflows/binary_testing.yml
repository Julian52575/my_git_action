name: binary-testing
on : [push]
jobs:
  compiling:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: /app

    steps:
      - env: ${{ github.action_path }}
        echo: MY_VAR
      - run: ls -l
      - run: make re
